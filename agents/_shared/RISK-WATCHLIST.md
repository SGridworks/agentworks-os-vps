# Risk Watchlist

Check these before release:

- public repo history and current tree contain no private data
- installer is pinned to a release tag
- Docker services bind only to intended interfaces
- smoke and E2E tests pass from a clean install
- docs match actual service behavior
