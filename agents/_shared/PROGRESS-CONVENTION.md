# Progress Convention

When reporting progress:

- Use `DONE` for completed and verified work.
- Use `PARTIAL` for implemented work with incomplete verification.
- Use `BLOCKED` for work requiring operator input or external access.
- Include the verification command or reason verification was skipped.
