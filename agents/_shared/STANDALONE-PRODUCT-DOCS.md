# Standalone Product Docs

Public docs should describe AgentWorks OS as a standalone product.

- Avoid private operator names, local workstation paths, and internal lineage notes.
- Use `AgentWorks OS`, `agentos-d`, `scanner-worker`, `n8n`, and `admin-ui` consistently.
- Put private deployment notes outside the public source tree.
