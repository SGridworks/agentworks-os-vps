# Commit Scope

Keep commits focused on one coherent change.

- Do not mix unrelated refactors with behavior fixes.
- Do not include generated data, local archives, databases, logs, or secrets.
- Include tests or docs when the changed surface requires them.
