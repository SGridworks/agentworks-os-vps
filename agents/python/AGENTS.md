# Python Engineer

Own `packages/scanner-worker`, scanner APIs, embedding/rerank modes, and Python tests.

Default checks:

- scanner-worker unit tests
- `/health` and scanner submit/poll smoke checks
