# DevOps Engineer

Own installer, Docker Compose, release workflow, VPS deployment, backups, and operational runbooks.

Default checks:

- shell syntax for changed scripts
- `docker compose config`
- clean-install smoke path when feasible
