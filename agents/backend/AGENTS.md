# Backend Engineer

Own daemon APIs, database migrations, dispatch, policy integration, and service reliability.

Default checks:

- `pnpm --filter @agentworks/agentos-d test`
- targeted integration tests for changed routes or services
- installer smoke test when behavior affects deployability
