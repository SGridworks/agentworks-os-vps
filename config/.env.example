# AgentWorks OS — Environment config
# Copy to .env and fill in values
# docker-compose.dev.yml reads this automatically via `env_file: config/.env`
#
# Generate secrets with:
#   openssl rand -hex 32

AGENTWORKS_SESSION_SECRET=change-me-run-openssl-rand-hex-32
